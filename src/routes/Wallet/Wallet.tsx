import React from 'react';
import styled from 'styled-components';

interface IProps {}

const Styled = styled.div``;

const Wallet = (props: IProps) => {
  return (
    <Styled>
      <p>Hello! This is page wallet</p>
    </Styled>
  );
};

export default Wallet;
