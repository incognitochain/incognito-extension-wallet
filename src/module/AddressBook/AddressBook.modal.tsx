import React from 'react';
import styled from 'styled-components';

interface IProps {}

const Styled = styled.div``;

const AddressBookModal = (props: IProps) => {
  return (
    <Styled>
      <p>fdsafdsfdsa</p>
    </Styled>
  );
};

export default AddressBookModal;
