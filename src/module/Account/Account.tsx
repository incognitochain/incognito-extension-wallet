import React from 'react';
import styled from 'styled-components';

interface IProps {}

const Styled = styled.div``;

const Account = (props: IProps) => {
  return <Styled className='account-container'></Styled>;
};

export default Account;
